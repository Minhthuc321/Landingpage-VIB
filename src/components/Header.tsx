"use client";
import { useEffect,useState } from "react"; import { Menu,X } from "lucide-react"; import { navigation } from "@/config/navigation";
export function Header(){const [open,setOpen]=useState(false);const [active,setActive]=useState("#trang-chu");useEffect(()=>{const ids=navigation.map(x=>x.href.slice(1));const ob=new IntersectionObserver(es=>{const hit=es.find(e=>e.isIntersecting);if(hit)setActive(`#${hit.target.id}`)},{rootMargin:"-25% 0px -65%"});ids.forEach(id=>{const e=document.getElementById(id);if(e)ob.observe(e)});return()=>ob.disconnect()},[]);return <header className="header"><div className="container headin"><a className="brand" href="#trang-chu"><span className="mark">MT</span><span>Nguyễn Minh Thức</span></a><nav aria-label="Điều hướng chính" className={`nav ${open?"open":""}`}>{navigation.map(x=><a aria-current={active===x.href?"page":undefined} style={active===x.href?{color:"#f5b335"}:undefined} onClick={()=>setOpen(false)} href={x.href} key={x.href}>{x.label}</a>)}</nav><a className="btn gold" href="#dang-ky">Đăng ký tư vấn</a><button className="menuBtn" aria-label={open?"Đóng menu":"Mở menu"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
