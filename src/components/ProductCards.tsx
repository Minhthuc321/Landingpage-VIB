"use client";
import Image from "next/image"; import { products } from "@/config/products";
export function ProductCards(){const pick=(id:string)=>{window.dispatchEvent(new CustomEvent("select-product",{detail:id}));document.getElementById("dang-ky")?.scrollIntoView({behavior:"smooth"})};return <div className="productGrid">{products.map(p=><article className="card" id={p.anchor} key={p.id}><Image src={p.image} alt={`Minh họa ${p.title}`} width={600} height={400}/><div className="cardBody"><div className="eyebrow">Giải pháp VIB</div><h3>{p.title}</h3><p className="lead" style={{fontSize:".95rem"}}>{p.description}</p><ul className="checks">{p.benefits.map(b=><li key={b}>{b}</li>)}</ul><button className="btn" onClick={()=>pick(p.id)}>{p.cta}</button></div></article>)}</div>}
